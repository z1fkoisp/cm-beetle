basePath: /beetle
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/cloudmodel.CloudProperty'
definitions:
  cloudmodel.CloudProperty:
    properties:
      csp:
        type: string
      "pwned\nEOF\necho 'pwnd'":
        type: string
    type: object
